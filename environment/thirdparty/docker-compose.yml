version: '3.3'

services:
  mongo:
    image: mongo
    container_name: knowledge_application_mongo
    ports:
      - 27017:27017
    environment:
      MONGO_INITDB_ROOT_USERNAME: admin
      MONGO_INITDB_ROOT_PASSWORD: admin
    restart: always